{# task/row.task.html.twig #}

<div class="col">
    <div class="form-row">
        <div class="col-3">{{ task.task_name }}</div>
        <div class="col-5">{{ task.user_email }}</div>
        <div class="col-2">{{ task.commit_length }}</div>
        <div class="col-2">{{ task.task_id }}</div>
    </div>
</div>